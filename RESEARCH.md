# 📊 RESEARCH: Tarot Mini App

**Версия:** 1.0  
**Дата:** 2026-03-02  
**Статус:** ✅ УТВЕРЖДЁН

---

## 1. НАЗНАЧЕНИЕ ПРОЕКТА

**Telegram Mini App для гадания на картах Таро**

### 1.1 Основная цель
Мистическое веб-приложение внутри Telegram для гадания на картах Таро с:
- Визуальным восприятием карт
- Плавными анимациями (120 Гц)
- Тактильным откликом
- Звуковым сопровождением
- Оплатой через Telegram Stars
- Админ-панелью для управления

### 1.2 Платформа
- **Telegram WebApp** — работает внутри Telegram
- **Адаптивный дизайн** — ПК / Мобильные устройства
- **Vanilla JavaScript** — без фреймворков (полный контроль)

---

## 2. ФУНКЦИИ ПРОЕКТА

### 2.1 Основной цикл взаимодействия

```
┌─────────────────────────────────────────────────────────┐
│                   ГЛАВНЫЙ ЭКРАН                         │
│                                                         │
│  Пользователь → Касание колоды → Тактильный отклик     │
│       ↓                                                 │
│  Тасовка карт (6 секунд) + Звук shuffle.mp3            │
│       ↓                                                 │
│  Вибрация (готово)                                      │
│       ↓                                                 │
│  Выбор расклада                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 Расклады

#### **Ежедневный расклад (3 карты)**

**Механика:**
1. Тасовка колоды (6 секунд)
2. Выбор 3 случайных карт
3. Переворот по тапу с частицами
4. Позиции:
   - Прошлое
   - Настоящее
   - Будущее
5. Кнопки действий:
   - «Поделиться с тарологом»
   - «Новый расклад»

**Данные карт:**
- 78 карт (22 Старших Аркана + 56 Младших)
- Изображения WebP
- Описание + ключевые слова

---

#### **Расклад «Путь» (7 карт, Хайо Банцхаф)**

**Механика:**
1. Тасовка колоды
2. **7-секундная концентрация** (песочные часы)
3. **Активация:**
   - Кнопка «Сделать расклад»
   - **ИЛИ** встряхивание телефона (акселерометр)
4. Схема расклада:
   ```
       1 (Суть вопроса)
     2   7
     3   6
     4   5
   ```
5. Позиции:
   - 1: Суть вопроса (центр)
   - 2-4: Прошлое (ментальный, астральный, физический)
   - 5-7: Будущее (физический, астральный, ментальный)

**Особенности:**
- Шахматное расположение карт
- Общее толкование с учётом позиций
- Анимация песочных часов (7 секунд)

---

### 2.3 Telegram Stars интеграция

**Поток пользователя:**

```
1. Расклад готов
       ↓
2. Кнопка «Поделиться с тарологом»
       ↓
3. Экран выбора таролога (3 карточки)
       ↓
4. Выбор таролога → Модальное окно оплаты
       ↓
5. Оплата через Telegram Stars API
       ↓
6. Успешная оплата → Чат с тарологом (25 мин)
       ↓
7. Таймер чата → Окончание консультации
       ↓
8. Экран оценки таролога (1-5 звёзд)
```

**Распределение платежа:**
- **10%** — разработчик (комиссия платформы)
- **90%** — таролог

**Динамическое ценообразование:**
```javascript
function calculatePrice(sessionsCompleted) {
  const level = Math.floor(sessionsCompleted / 10) + 1;
  const price = 33 * Math.pow(1.1, level - 1);
  return Math.min(Math.round(price), 333); // Максимум 333 ⭐
}
```

---

### 2.4 Админ-панель

**Функционал:**

#### Вкладка 1: Статистика
- Общий доход (звёзды)
- Комиссия 10% (звёзды)
- Количество тарологов
- Количество консультаций
- Баланс к выплате

#### Вкладка 2: Тарологи
- Список всех тарологов
- Баланс каждого
- Рейтинг + количество оценок
- Добавить таролога
- Редактировать / Удалить

#### Вкладка 3: Выплаты
- Список выплат
- Создать выплату
- Отметить выплату
- Отменить выплату

#### Вкладка 4: Возвраты (Refund)
- Список транзакций
- Отмена оплаты
- Возврат средств через Telegram API

---

## 3. ДИЗАЙН ПРОЕКТА

### 3.1 Цветовая палитра

```css
:root {
  /* Фон */
  --bg-color: #1a0b2e;          /* Глубокий фиолетовый */
  --bg-gradient: linear-gradient(
    135deg, 
    #1a0b2e 0%, 
    #2d1f4e 50%, 
    #1a0b2e 100%
  );

  /* Акцент */
  --gold: #d4af37;              /* Золото */
  --gold-light: #f4d06f;        /* Светлое золото */

  /* Текст */
  --text-primary: #f5f5f5;      /* Основной */
  --text-secondary: rgba(245, 245, 245, 0.7);  /* Вторичный */
}
```

---

### 3.2 Шрифты

**Заголовки:**
```css
font-family: 'Cormorant Garamond', serif;
font-weight: 400, 600, 700;
```

**Магические заголовки:**
```css
font-family: 'Cinzel', serif;
```

**Основной текст:**
```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Georgia, serif;
```

**Подключение:**
```html
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600;700&family=Cinzel:wght@400;600;700&display=swap" rel="stylesheet">
```

---

### 3.3 Звуки

**Файл:** `public/assets/sounds/shuffle.mp3`

**Особенности:**
- «Разогрев» аудио при первом взаимодействии (для мобильных)
- Воспроизведение при тасовке колоды
- Отключение в настройках (шестерёнка)
- Обработка ошибок загрузки

**Использование:**
```javascript
import { playShuffleSound, warmupAudio } from './sound.js';

// "Разогрев" при первом клике
document.addEventListener('click', warmupAudio, { once: true });

// Воспроизведение при тасовке
if (state.soundEnabled) {
  playShuffleSound();
}
```

---

### 3.4 Анимации

#### **Переворот карты**
```css
.card-inner {
  transition: transform 0.7s cubic-bezier(0.4, 0.0, 0.2, 1);
  transform-style: preserve-3d;
}

.card.flipped .card-inner {
  transform: rotateY(180deg);
}
```

**Особенности:**
- `cubic-bezier` для плавности
- `transform: rotateY(180deg)` для 3D эффекта
- `will-change: transform` для 120 Гц

---

#### **Частицы при перевороте (Canvas)**

**Параметры:**
- 20 частиц на переворот
- Цвет: золотой (`hsl(45-60, 80%, 60-80%)`)
- Физика: гравитация (`vy += 0.1`)
- Затухание: `decay 0.02-0.04`
- Время жизни: ~2 секунды

**Код:**
```javascript
import { createParticles } from './animations.js';

setTimeout(() => {
  const rect = cardEl.getBoundingClientRect();
  createParticles(
    rect.left + rect.width / 2,
    rect.top + rect.height / 2
  );
}, 350); // Середина анимации
```

---

#### **Звёздный фон (Canvas)**

**Параметры:**
- Canvas на весь экран
- Количество звёзд: `area / 4000`
- Мерцание через `sin(phase)`
- Скорость: 0.01-0.03

**Код:**
```javascript
import { initStars } from './animations.js';

initStars(); // Инициализация при загрузке
```

---

#### **Песочные часы (CSS Keyframes)**

**Длительность:** 7 секунд

**Код:**
```css
@keyframes flip-hourglass {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(180deg); }
}

.hourglass {
  animation: flip-hourglass 7s linear forwards;
}
```

---

### 3.5 Адаптивность

| Размер экрана | Карты | Особенности |
|---------------|-------|-------------|
| ≤360px | 90×144px | `scale(0.85)`, уменьшенные отступы |
| Основной | 100×160px | Базовые размеры |
| ≥768px | 120×192px | Увеличенные размеры |

**CSS:**
```css
@media (max-width: 360px) {
  .card {
    transform: scale(0.85);
  }
}

@media (min-width: 768px) {
  .card {
    width: 120px;
    height: 192px;
  }
}
```

---

## 4. АРХИТЕКТУРА (C4 Model)

### 4.1 Context Diagram

```
┌──────────────┐         ┌──────────────────┐         ┌──────────────┐
│   Пользователь│◄───────►│  Telegram Client │◄───────►│  Tarot WebApp│
└──────────────┘         └──────────────────┘         └───────┬──────┘
                                                               │
                    ┌──────────────────────────────────────────┘
                    │
         ┌──────────▼──────────┐              ┌─────────────────────┐
         │   Backend Server    │◄────────────►│   SQLite Database   │
         │  (Express + Socket) │              │   (tarot.db)        │
         └──────────┬──────────┘              └─────────────────────┘
                    │
         ┌──────────▼─────────────────────────────────────┐
         │              External Services                  │
         ├─────────────────┬──────────────┬───────────────┤
         │  Telegram Bot   │  Telegram    │  GitHub Pages │
         │  API (Payments) │  WebApp API  │  (CDN/Static) │
         └─────────────────┴──────────────┴───────────────┘
```

---

### 4.2 Container Diagram (Логическое разделение)

```
┌──────────────────────────────────────────────────────────┐
│              ОБЩАЯ БАЗА ДАННЫХ (SQLite)                  │
│  users, transactions, events, tarologists, payouts       │
└──────────────────────────────────────────────────────────┘
           ▲                              ▲
           │                              │
    ┌──────┴─────────┐            ┌───────┴────────┐
    │  ОСНОВНОЕ      │            │   АДМИНКА      │
    │  ПРИЛОЖЕНИЕ    │            │   (Admin SPA)  │
    │                │            │                │
    │  public/       │            │  public/admin/ │
    │  ├── index.html│            │  ├── index.html│
    │  ├── js/       │            │  ├── js/       │
    │  │   ├── main.js│           │  │   ├── admin.js│
    │  │   ├── cards.js│          │  │   ├── refund.js│
    │  │   └── ...   │            │  │   └── ...   │
    │  └── css/      │            │  └── css/      │
    └────────────────┘            └────────────────┘
    
    ПРИНЦИП:
    ✅ Правка main.js НЕ влияет на admin.js
    ✅ Правка admin.js НЕ влияет на main.js
    ✅ Общая БД и переменные окружения
    ✅ Единый дизайн (CSS variables)
```

---

## 5. ТЕКУЩИЙ СТАТУС (Plan)

| Этап | Описание | Статус | Проблемы |
|------|----------|--------|----------|
| 1 | Основное приложение (расклады) | ✅ Реализовано | 🐛 Telegram WebApp кэш |
| 2 | Оплата Stars | ✅ Реализовано | ⏳ Тестирование |
| 3 | Админ-панель | 🟡 Частично | 🐛 Nginx X-Frame-Options |
| 4 | Трекинг событий | ✅ Реализовано | — |
| 5 | Статистика (DAU, LTV) | ✅ Реализовано | — |
| 6 | CRUD тарологов | ✅ Реализовано | — |
| 7 | Выплаты и возвраты | ✅ Реализовано | — |

---

## 6. ПРОТОКОЛ ВЗАИМОДЕЙСТВИЯ

### 6.1 Роли

**Qwen Code (локально):**
- ✅ Пишет код
- ✅ Коммитит и пушит на GitHub
- ✅ Создаёт инструкции в `exchange/`
- ✅ Читает отчёты из `toQwen.md`
- ❌ НЕ подключается к серверу для деплоя

**Opencode Agent (VDS):**
- ✅ Загружает код через `git pull`
- ✅ Деплоит и тестирует
- ✅ Пишет отчёты в `toQwen.md`
- ❌ НЕ пишет код без инструкции

---

### 6.2 Кибернетическая петля

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Qwen Code: Пишет код → Commit → Push → Tag "WIP"     │
│       ↓                                                 │
│  Opencode Agent: Git Pull → Deploy → Test              │
│       ↓                                                 │
│  ┌──────────────────────────────────────────────────┐  │
│  │  ТЕСТЫ ПРОЙДЕНЫ?                                 │  │
│  │     ↓ YES → Менеджер подтверждает → ГОТОВО       │  │
│  │     ↓ NO                                         │  │
│  │     ↓                                              │  │
│  │  ┌────────────────────────────────────────────┐  │  │
│  │  │  МИКРОЦИКЛ ДИАГНОСТИКИ                     │  │  │
│  │  │                                             │  │  │
│  │  │  1. Git diff с рабочей версией             │  │  │
│  │  │  2. Откат → Тест                           │  │  │
│  │  │  3. Если помогло → Исправить               │  │  │
│  │  │  4. Если нет → Глубокая диагностика        │  │  │
│  │  └────────────────────────────────────────────┘  │  │
│       ↓                                              │  │
│  Opencode Agent: Отчёт (toQwen.md)                  │  │
│       ↓                                              │  │
│  Qwen Code: Ревью → Исправления → Repeat            │  │
│       ↓                                              │  │
│  Менеджер: Подтверждение → Tag "RELEASE"            │  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Петля НЕ завершается пока:**
- ❌ Код не работает
- ❌ Тесты не пройдены
- ❌ Менеджер не подтвердил

---

### 6.3 Микроцикл диагностики (Сравнение версий)

**Критерии РАБОТАЮЩЕЙ версии кода:**

```
✅ Код выгружен Qwen Code на GitHub
✅ Задеплоен Opencode Agent на сервер
✅ Протестирован Opencode Agent (все тесты ✅)
✅ Протестирован Менеджером (в приложении + web + desktop)
✅ Зафиксирован в toQwen.md как "ГОТОВО"
✅ Помечен Git Tag "RELEASE"
```

---

**Алгоритм при обнаружении бага:**

```bash
# 1. Найти последнюю рабочую версию
git log --oneline | grep "RELEASE"
# или
git tag -l "RELEASE*"

# 2. Сравнить с текущей версией
git diff RELEASE-1.0..HEAD -- path/to/file.js

# 3. Если найдены изменения → Откатить → Тестировать
git checkout RELEASE-1.0 -- path/to/file.js

# 4. Если помогло → Исправить → Закоммитить
# 5. Если нет → Глубокая диагностика (логи, консоль)
```

---

**Пример использования:**

```
СИТУАЦИЯ: После обновления кнопки админки не работают

ШАГИ:
1. Opencode Agent сообщает о баге в toQwen.md
2. Qwen Code находит последний RELEASE:
   git tag -l "RELEASE*" → RELEASE-1.2
3. Сравнивает изменения:
   git diff RELEASE-1.2..HEAD -- public/js/admin.js
4. Находит изменение в setupEventListeners()
5. Откатывает:
   git checkout RELEASE-1.2 -- public/js/admin.js
6. Тестирует → Работает!
7. Исправляет код → Коммит → Push
8. Новый цикл тестирования
```

---

**Преимущества микроцикла:**

| Преимущество | Описание |
|--------------|----------|
| Быстрая локализация | Сразу видно что сломалось |
| Объективный критерий | Код либо работает, либо нет |
| Автоматизация | `git diff` покажет изменения |
| Восстановление | Можно откатиться к рабочей версии |
| Обучение | Запоминание ошибок для будущего |

---

**Ограничения:**

| Ограничение | Описание |
|-------------|----------|
| Не все баги от изменений | Некоторые баги были скрыты |
| Контекст теряется | Почему изменения были внесены |
| Может замедлить | Постоянные сравнения |

---

**РЕШЕНИЕ:** Использовать микроцикл КАК ОПЦИЮ при критических багах.

---

## 7. СТРУКТУРА ПРОЕКТА (МОДУЛЬНАЯ АРХИТЕКТУРА)

**Принцип:** Один репозиторий — логическое разделение на блоки

```
miniapp/
├── public/
│   │
│   ├── core/                      # ← ОБЩИЕ МОДУЛИ (ядро)
│   │   ├── animations.js          # Canvas (звёзды, частицы)
│   │   ├── sound.js               # Звук тасовки
│   │   ├── shake.js               # Встряхивание
│   │   └── utils.js               # Утилиты
│   │
│   ├── main-app/                  # ← БЛОК 1: Основное приложение
│   │   ├── index.html             # Главная страница
│   │   ├── main.js                # Точка входа, навигация
│   │   ├── cards.js               # Данные 78 карт
│   │   └── spreads.js             # Логика раскладов
│   │
│   ├── modules/                   # ← БЛОК 2: Тарологи + Оплата
│   │   ├── tarologists/
│   │   │   ├── tarologists.js     # Выбор таролога + Mock
│   │   │   └── chat.js            # Чат с тарологом
│   │   └── payment/
│   │       ├── payment.js         # Оплата (Telegram Stars)
│   │       └── invoice.js         # Инвойсы
│   │
│   ├── admin-app/                 # ← БЛОК 3: Админ-панель
│   │   ├── admin.html             # Страница админки
│   │   ├── admin.js               # Логика админки
│   │   └── refund.js              # Возвраты средств
│   │
│   ├── css/                       # ← ОБЩИЕ СТИЛИ
│   │   ├── style.css              # Основные стили (2086 строк)
│   │   ├── admin.css              # Стили админки
│   │   └── modules.css            # Стили модулей
│   │
│   └── assets/                    # ← ОБЩИЕ РЕСУРСЫ
│       ├── card-backs/            # Рубашки карт
│       ├── card-faces/            # Изображения карт
│       └── sounds/                # Звуки
│
├── server/                        # ← ОБЩИЙ BACKEND
│   ├── server.js                  # Express + WebSocket
│   ├── admin-bot.js               # Бот админки
│   ├── db.js                      # SQLite модели
│   └── .env                       # Переменные окружения
│
├── PROTOCOL.md                    # Протокол взаимодействия
├── RESEARCH.md                    # Этот документ
└── ...
```

---

### 7.1 Логическое разделение блоков

| Блок | Назначение | Зависимости | Влияние |
|------|------------|-------------|---------|
| **core/** | Общие модули | Нет | Используется всеми |
| **main-app/** | Расклады, карты | core/ | Не влияет на другие |
| **modules/** | Тарологи, оплата | core/, main-app/ | Не влияет на другие |
| **admin-app/** | Админка | core/, server/ | Не влияет на другие |
| **server/** | API, БД | Нет | Общее для всех |

---

### 7.2 Принципы модульности

**✅ Преимущества:**

| Принцип | Описание |
|---------|----------|
| **Единый репозиторий** | Один `git push` — все блоки обновляются |
| **Логическое разделение** | Папки по функционалу |
| **Общие модули** | `core/` используется всеми блоками |
| **Независимость** | Правка `main-app/` НЕ влияет на `modules/` |
| **Масштабируемость** | Легко добавить новый модуль |
| **Простота деплоя** | Один деплой — всё работает |

---

**❌ Избегаем:**

| Ошибка | Почему плохо |
|--------|--------------|
| Полное разделение (3 репозитория) | Сложность деплоя, версионность |
| Дублирование `core/` | 3 копии animations.js |
| Жёсткие зависимости | modules/ зависит от main-app/ |
| Смешивание логики | payment.js в main.js |

---

### 7.3 Пример добавления нового модуля

**Задача:** Добавить модуль "История раскладов"

**Шаги:**

```bash
# 1. Создать папку модуля
mkdir public/modules/history

# 2. Добавить файлы модуля
touch public/modules/history/history.js
touch public/modules/history/history.css

# 3. Подключить в main.js
import { showHistory } from './modules/history/history.js';

# 4. Коммит
git add -A
git commit -m "feat: модуль истории раскладов"
git push origin main
```

**Результат:**
- ✅ Модуль изолирован
- ✅ Использует `core/`
- ✅ Не влияет на другие блоки
- ✅ Один деплой

---

### 7.4 Миграция текущей структуры

**Текущее состояние:**

```
public/
├── js/
│   ├── main.js              # СМЕШАНО: расклады + навигация
│   ├── tarologists.js       # СМЕШАНО: выбор + оплата
│   └── chat.js              # Чат
└── ...
```

**Целевое состояние:**

```
public/
├── core/
│   ├── animations.js        # Вынесено из js/
│   ├── sound.js             # Вынесено из js/
│   └── shake.js             # Вынесено из js/
│
├── main-app/
│   ├── main.js              # Только расклады
│   ├── cards.js             # Перемещено из js/
│   └── spreads.js           # Перемещено из js/
│
├── modules/
│   ├── tarologists/
│   │   └── tarologists.js   # Только выбор
│   └── payment/
│       └── payment.js       # Вынесено из tarologists.js
│
└── admin-app/
    └── admin.js             # Уже отдельно
```

---

**План миграции:**

| Этап | Действия | Статус |
|------|----------|--------|
| 1 | Создать папки `core/`, `main-app/`, `modules/` | ⏳ Ожидает |
| 2 | Переместить файлы | ⏳ Ожидает |
| 3 | Обновить импорты | ⏳ Ожидает |
| 4 | Тестирование | ⏳ Ожидает |
| 5 | Деплой | ⏳ Ожидает |

---

**Статус:** 🟡 **В ПРОЦЕССЕ** (требуется рефакторинг)

---

## 8. ПРОТОКОЛ СИНХРОНИЗАЦИИ КОДА

**Версия:** 1.0  
**Дата:** 2026-03-04  
**Статус:** ✅ УТВЕРЖДЁН

---

### 8.1 НАЗНАЧЕНИЕ

**Протокол синхронизации** — обязательная процедура синхронизации кода между:
- **MacBook** (локальная разработка Qwen Code)
- **Git** (GitHub репозиторий)
- **Сервер** (VDS, Opencode Agent)

**Цель:** Исключить рассинхронизацию после исправления багов.

---

### 8.2 КОГДА ПРИМЕНЯТЬ

**Обязательно после:**
- ✅ Исправления критических багов
- ✅ Внесения изменений несколькими агентами
- ✅ Нарушения протокола (Opencode Agent делал изменения на сервере)
- ✅ Перед финальным деплоем

---

### 8.3 ПРОЦЕДУРА СИНХРОНИЗАЦИИ

#### **Шаг 1: Сравнение файлов**

**Qwen Code выполняет команды:**

```bash
# 1. main.js:17 (optional chaining)
echo "=== main.js:17 ==="
sed -n '17p' public/js/main.js
git show HEAD:public/js/main.js | sed -n '17p'
ssh root@server "sed -n '17p' /var/www/tarot-miniapp/public/js/main.js"

# 2. tarologists.js:6 (getCurrentSpread)
echo "=== tarologists.js:6 ==="
sed -n '6p' public/js/tarologists.js
git show HEAD:public/js/tarologists.js | sed -n '6p'
ssh root@server "sed -n '6p' /var/www/tarot-miniapp/public/js/tarologists.js"

# 3. admin.js:13 (ADMIN_TELEGRAM_ID)
echo "=== admin.js:13 ==="
sed -n '13p' public/js/admin.js
git show HEAD:public/js/admin.js | sed -n '13p'
ssh root@server "sed -n '13p' /var/www/tarot-miniapp/public/js/admin.js"

# 4. refund.js (последние 3 строки)
echo "=== refund.js ==="
tail -3 public/js/refund.js
git show HEAD:public/js/refund.js | tail -3
ssh root@server "tail -3 /var/www/tarot-miniapp/public/js/refund.js"

# 5. admin.js:28-40 (DOM переменные)
echo "=== admin.js:28-40 ==="
sed -n '28,40p' public/js/admin.js
git show HEAD:public/js/admin.js | sed -n '28,40p'
ssh root@server "sed -n '28,40p' /var/www/tarot-miniapp/public/js/admin.js"
```

---

#### **Шаг 2: Выявление расхождений**

**Сравнить вывод:**

| Результат | Действие |
|-----------|----------|
| ✅ Все 3 версии идентичны | Перейти к Шагу 4 |
| ❌ Есть расхождения | Перейти к Шагу 3 |

---

#### **Шаг 3: Синхронизация**

**Если сервер имеет правильную версию:**

```bash
# 1. Скачать файл с сервера
ssh root@server "cat /var/www/tarot-miniapp/public/js/admin.js" > /tmp/server_admin.js

# 2. Копировать на MacBook
cp /tmp/server_admin.js public/js/admin.js

# 3. Проверить
diff /tmp/server_admin.js public/js/admin.js
# Должно быть пусто (нет различий)
```

**Если MacBook имеет правильную версию:**

```bash
# 1. Исправить файл на MacBook
# 2. Закоммитить и запушить
git add -A
git commit -m "fix: синхронизация с сервером"
git push origin main

# 3. Opencode Agent делает git pull
```

---

#### **Шаг 4: Финальная проверка**

**Qwen Code выполняет:**

```bash
echo "=== ФИНАЛЬНАЯ ПРОВЕРКА ==="

# main.js:17
ssh root@server "sed -n '17p' /var/www/tarot-miniapp/public/js/main.js"
git show HEAD:public/js/main.js | sed -n '17p'

# tarologists.js:6
ssh root@server "sed -n '6p' /var/www/tarot-miniapp/public/js/tarologists.js"
git show HEAD:public/js/tarologists.js | sed -n '6p'

# admin.js:13
ssh root@server "sed -n '13p' /var/www/tarot-miniapp/public/js/admin.js"
git show HEAD:public/js/admin.js | sed -n '13p'

# refund.js
ssh root@server "tail -3 /var/www/tarot-miniapp/public/js/refund.js"
git show HEAD:public/js/refund.js | tail -3

echo "✅ ВСЕ ФАЙЛЫ СИНХРОНИЗИРОВАНЫ!"
```

---

#### **Шаг 5: Коммит и пуш**

**Если были изменения:**

```bash
git add -A
git commit -m "fix: синхронизация кода (MacBook ↔ Git ↔ Сервер)

🔄 SYNC: Протокол синхронизации v1.0

Синхронизированы файлы:
- main.js: optional chaining ✅
- tarologists.js: getCurrentSpread ✅
- admin.js: let переменные ✅
- refund.js: без дубликатов ✅

Статус: ✅ Полностью синхронизировано"
git push origin main
```

---

### 8.4 ЧЕК-ЛИСТ СИНХРОНИЗАЦИИ

**Перед деплоем проверить:**

```
□ main.js:17 — import.meta?.env?.DEV
□ tarologists.js:6 — getCurrentSpread
□ admin.js:13 — let ADMIN_TELEGRAM_ID
□ admin.js:28-40 — let screens = null (без дубля)
□ refund.js:118 — без дубликата экспорта
□ Все 3 версии идентичны (MacBook, Git, Сервер)
```

---

### 8.5 ТАБЛИЦА СИНХРОНИЗАЦИИ

| Файл | Критическая строка | Правильное значение |
|------|-------------------|---------------------|
| main.js | 17 | `if (import.meta?.env?.DEV) {` |
| tarologists.js | 6 | `import { ..., getCurrentSpread, ... }` |
| admin.js | 13 | `let ADMIN_TELEGRAM_ID = null;` |
| admin.js | 28-40 | `let screens = null;` (без `const`) |
| refund.js | 118 | `// Экспорт` (без дубликата) |

---

### 8.6 НАРУШЕНИЯ ПРОТОКОЛА

**Если Opencode Agent делал изменения на сервере:**

1. ✅ **Скачать файл с сервера**
2. ✅ **Сравнить с Git и MacBook**
3. ✅ **Выбрать правильную версию**
4. ✅ **Синхронизировать все 3 версии**
5. ✅ **Закоммитить и запушить**

---

**Протокол вступает в силу:** 2026-03-04  
**Версия:** 1.0  
**Статус:** ✅ ОБЯЗАТЕЛЕН К ПРИМЕНЕНИЮ

---

**ДОКУМЕНТ УТВЕРЖДЁН:** 2026-03-04
**ВЕРСИЯ:** 1.0 (с Протоколом синхронизации)
**СТАТУС:** ✅ ДЕЙСТВУЮЩИЙ
