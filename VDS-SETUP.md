# 🚀 Настройка VDS для Tarot Mini App

## 📋 Требования

- **ОС:** Ubuntu 22.04
- **Домен:** Куплен и направлен на IP сервера
- **Доступ:** SSH доступ к серверу

---

## 🔧 Шаг 1: Подключение к серверу

```bash
# Подключитесь к серверу
ssh user@your-vds-ip
```

---

## 🔧 Шаг 2: Загрузка скрипта настройки

```bash
# Перейдите в домашнюю директорию
cd ~

# Скачайте скрипт (замените ссылку на ваш репозиторий)
wget https://raw.githubusercontent.com/adolfass/miniapp_taro/main/setup-vds.sh

# Или скопируйте через scp с локальной машины:
# scp setup-vds.sh user@your-vds-ip:~/
```

---

## 🔧 Шаг 3: Запуск настройки

```bash
# Сделайте скрипт исполняемым
chmod +x setup-vds.sh

# Запустите настройку
./setup-vds.sh
```

**Скрипт выполнит:**
1. ✅ Обновление системы
2. ✅ Установка Node.js 20 LTS
3. ✅ Установка Nginx
4. ✅ Создание директории `/var/www/tarot-miniapp`
5. ✅ Настройка Nginx конфигурации
6. ✅ Установка Let's Encrypt (HTTPS)
7. ✅ Настройка UFW фаервола
8. ✅ Создание скрипта деплоя

---

## 🔧 Шаг 4: Редактирование конфигурации

После запуска скрипта отредактируйте конфиг Nginx:

```bash
sudo nano /etc/nginx/sites-available/tarot-miniapp
```

**Замените:**
- `YOUR_DOMAIN.COM` → `ваш-домен.com`
- `your-email@example.com` → `ваш email`

**Сохраните:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

## 🔧 Шаг 5: Первый деплой

```bash
# Запустите скрипт деплоя
sudo /var/www/deploy.sh
```

Скрипт:
1. Склонирует репозиторий
2. Установит зависимости
3. Соберёт проект
4. Перезагрузит Nginx

---

## 🔧 Шаг 6: Проверка

Откройте в браузере:
```
https://ваш-домен.com
```

**Что проверить:**
- [ ] Сайт загружается по HTTPS
- [ ] Замочек в адресной строке
- [ ] Карты Таро отображаются
- [ ] Анимации работают
- [ ] Мобильная версия корректна

---

## 📊 Полезные команды

### Логи Nginx
```bash
# Просмотр логов в реальном времени
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### Статус Nginx
```bash
sudo systemctl status nginx
```

### Перезапуск Nginx
```bash
sudo systemctl restart nginx
```

### Обновление приложения
```bash
sudo /var/www/deploy.sh
```

---

## 🔒 Безопасность

После настройки:

```bash
# Проверка фаервола
sudo ufw status

# Должно быть:
# Status: active
# To                         Action      From
# --                         ------      ----
# OpenSSH                    ALLOW       Anywhere
# Nginx Full                 ALLOW       Anywhere
```

---

## 🆘 Если что-то пошло не так

### Ошибка при получении сертификата
```bash
# Проверьте, что домен указывает на сервер
ping ваш-домен.com

# Должен вернуться IP вашего сервера
```

### Ошибка Nginx
```bash
# Проверка конфигурации
sudo nginx -t

# Перезагрузка с проверкой
sudo systemctl reload nginx
```

### Ошибка при деплое
```bash
# Проверьте логи
cd /var/www/tarot-miniapp
npm run build 2>&1 | tee build.log
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `/var/log/nginx/error.log`
2. Проверьте статус: `sudo systemctl status nginx`
3. Перезапустите сервисы: `sudo systemctl restart nginx`

---

## ✅ Чек-лист успешной настройки

- [ ] Ubuntu 22.04 установлена
- [ ] Node.js 20.x установлен
- [ ] Nginx запущен
- [ ] HTTPS сертификат получен
- [ ] Фаервол настроен
- [ ] Сайт открывается по HTTPS
- [ ] Скрипт деплоя работает
- [ ] Telegram Mini App загружается

---

**После успешного деплоя можно приступать к интеграции оплаты звёздами!** ⭐
