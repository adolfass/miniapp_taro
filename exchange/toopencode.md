# 🐛 СРОЧНО: Исправление проблем с онлайн-статусом и кнопкой админки

**Версия протокола:** 5.7.0
**Дата:** 2026-03-10
**Приоритет:** 🔴 КРИТИЧНЫЙ
**Статус:** ⏳ ОЖИДАЕТ ИСПРАВЛЕНИЯ

---

## 🚨 ПРОБЛЕМЫ ОТ МЕНЕДЖЕРА

**Менеджер протестировал приложение и нашёл 3 проблемы:**

---

### ПРОБЛЕМА 1: Несоответствие онлайн-статуса

**Описание:**
- В приложении таролог "D P" отображается **онлайн** (зелёный)
- Но после нажатия кнопки "Готов консультировать" в боте - статус НЕ меняется
- **Ожидание:** Кнопка в боте должна менять статус на "онлайн"
- **Реальность:** Статус НЕ соответствует действительности

**Где смотреть:**
- Скриншот 1: `scp/Снимок экрана 2026-03-10 в 17.46.51.png` - интерфейс пользователя
- Скриншот 2: `scp/Снимок экрана 2026-03-10 в 17.47.02.png` - кнопка в боте

**Причина (возможная):**
- Функция `isRealOnline()` использует 5-минутное окно для WebSocket heartbeat
- Кнопка "Готов консультировать" устанавливает `ready_until` на 30 минут
- **НО:** `isRealOnline()` может НЕ учитывать `ready_until` правильно

**Проверить:**
```javascript
// В server/db.js
isRealOnline(id) {
  // ...
  // 3. Проверка ручного статуса (последние 30 минут)
  if (tarologist.ready_until) {
    const readyDiff = (now - new Date(tarologist.ready_until)) / 1000 / 60;
    if (readyDiff <= 0) {  // ← ПРАВИЛЬНО: должно быть >= 0
      return true;
    }
  }
}
```

---

### ПРОБЛЕМА 2: Пропала кнопка "Админ-панель" в боте

**Описание:**
- Менеджер (Telegram ID: 511017697) НЕ видит кнопку "🔐 Открыть админ-панель" в боте
- **Ожидание:** Админ должен видеть кнопку админки
- **Реальность:** Кнопка пропала

**Где смотреть:**
- `server/admin-bot.js` - функция `handleStart()` или `handleCommand()`

**Причина (возможная):**
- Менеджер одновременно является **и админом, и тарологом** (ID 7 - Роман)
- Логика бота может НЕ показывать кнопку админки если пользователь - таролог
- **Баг:** Проверка роли "админ" vs "таролог" конфликтует

**Проверить:**
```javascript
// В handleStart() или getUpdates()
if (userId === ADMIN_ID) {
  // Показать кнопку админки
} else if (tarologist) {
  // Показать кнопку таролога
}

// ← ПРОБЛЕМА: else if НЕ показывает обе кнопки!
```

---

### ПРОБЛЕМА 3: Конфликт ролей (Админ + Таролог)

**Описание:**
- Менеджер является **и администратором, и тарологом** одновременно
- Это может приводить к конфликтам в логике приложения

**Вопросы:**
1. Должен ли админ-таролог видеть **обе кнопки** (админка + таролог)?
2. Какой приоритет если роли конфликтуют?
3. Как `isRealOnline()` обрабатывает админ-таролога?

**Предлагаемое решение:**
- **Админ-таролог видит комбинированное меню:**
  ```
  [🔐 Открыть админ-панель]
  [💬 Мои чаты]
  [🟢 Готов консультировать (30 мин)]
  [💰 Для тарологов]
  ```

---

## 🔧 ЗАДАЧИ ДЛЯ ИСПРАВЛЕНИЯ

### Задача 1: Исправить isRealOnline()

**Файл:** `server/db.js`

**Проверить:**
```javascript
isRealOnline(id) {
  const tarologist = this.getById(id);
  if (!tarologist || !tarologist.is_active) {
    return false;
  }
  
  const now = new Date();
  
  // 1. WebSocket heartbeat (5 минут)
  if (tarologist.last_ws_ping) {
    const wsDiff = (now - new Date(tarologist.last_ws_ping)) / 1000 / 60;
    if (wsDiff <= 5) return true;
  }
  
  // 2. HTTP heartbeat (5 минут)
  if (tarologist.last_heartbeat_at) {
    const heartbeatDiff = (now - new Date(tarologist.last_heartbeat_at)) / 1000 / 60;
    if (heartbeatDiff <= 5) return true;
  }
  
  // 3. Ручной статус "Готов" (30 минут) ← ПРОВЕРИТЬ!
  if (tarologist.ready_until) {
    const readyDiff = (now - new Date(tarologist.ready_until)) / 1000 / 60;
    if (readyDiff <= 0) return true;  // ← Правильно?
  }
  
  // 4. Активная сессия чата
  const session = ChatSession.getActiveByTarologist(id);
  if (session) return true;
  
  return false;
}
```

**Ожидаемое поведение:**
- Если таролог нажал "Готов консультировать" → онлайн 30 минут
- Если WebSocket heartbeat < 5 минут → онлайн
- Если активная сессия → онлайн

---

### Задача 2: Исправить кнопку админки для админ-таролога

**Файл:** `server/admin-bot.js`

**Проблема:**
```javascript
// СЕЙЧАС (неправильно):
if (tarologist) {
  // Меню таролога
} else if (userId === ADMIN_ID) {
  // Меню админа
}
```

**Решение:**
```javascript
// ПРАВИЛЬНО:
const isAdmin = userId === ADMIN_ID;
const isTarologist = Tarologist.getByTelegramId(userId.toString());

if (isAdmin && isTarologist) {
  // КОМБИНИРОВАННОЕ меню (обе кнопки)
} else if (isTarologist) {
  // Меню таролога
} else if (isAdmin) {
  // Меню админа
} else {
  // Меню пользователя
}
```

---

### Задача 3: Протестировать на реальных данных

**Проверить:**
1. ✅ Менеджер (ID 511017697) видит **обе кнопки**
2. ✅ Кнопка "Готов консультировать" меняет статус на онлайн
3. ✅ Статус отображается правильно в приложении

**Команды для проверки:**
```bash
# Проверить статус менеджера в БД
sqlite3 /var/www/tarot-miniapp/server/tarot.db "SELECT id, name, is_active, last_ws_ping, ready_until FROM tarologists WHERE telegram_id = '511017697';"

# Проверить isRealOnline() через API
curl https://goldtarot.ru/api/tarologist/7/status

# Проверить логи бота
pm2 logs tarot-app --lines 50 | grep -i "callback_query\|tarologist_info"
```

---

## 📊 ОТЧЁТ

**Напиши в `exchange/toQwen.md` (добавь в конец файла):**

```markdown
---

# ✅ ОТЧЁТ: Исправление онлайн-статуса и кнопки админки

**Версия протокола:** 5.7.1
**Дата:** 2026-03-10
**Статус:** ✅ ВЫПОЛНЕНО
**Исполнитель:** Opencode Agent (SRE/DevOps)

---

## Исправлено:

| Проблема | Статус | Решение |
|----------|--------|---------|
| Несоответствие онлайн-статуса | ✅/❌ | |
| Пропала кнопка админки | ✅/❌ | |
| Конфликт ролей админ/таролог | ✅/❌ | |

---

## Изменения в коде:

**Файл:** `server/db.js`
```javascript
// Вставь исправленный код isRealOnline()
```

**Файл:** `server/admin-bot.js`
```javascript
// Вставь исправленную логику меню
```

---

## Проверка:

**Статус менеджера (ID 511017697):**
```
[вставь результат SQL запроса]
```

**API статус:**
```
[вставь результат curl]
```

**Кнопки в боте:**
- [ ] 🔐 Админ-панель ✅/❌
- [ ] 💬 Мои чаты ✅/❌
- [ ] 🟢 Готов консультировать ✅/❌
- [ ] 💰 Для тарологов ✅/❌

---

## Итог:

**Онлайн-статус работает:** ✅/❌
**Кнопка админки видна:** ✅/❌
**Конфликт ролей решён:** ✅/❌

---

**Выполнил:** Opencode Agent (SRE/DevOps)
**Время выполнения:** ~15 минут
**Сервер:** goldtarot.ru
**Статус:** 🟢 ONLINE

---

*Дата: 2026-03-10*
```

**После записи отчёта:**
```bash
cd /var/www/tarot-miniapp
git pull origin main
git add exchange/toQwen.md server/db.js server/admin-bot.js
git commit -m "fix: онлайн-статус и кнопка админки для админ-таролога"
git push origin main
```

---

## ⏳ ЖДУ КОМАНДУ МЕНЕДЖЕРА

**НЕ принимать отчёт пока менеджер не скажет:**
> "Проблемы исправлены, принимаю отчёт"

---

*Дата: 2026-03-10*
*Приоритет: 🔴 КРИТИЧНЫЙ*
*Версия протокола: 5.7.0*
