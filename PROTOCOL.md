# 📋 ПРОТОКОЛ ВЗАИМОДЕЙСТВИЯ: Qwen Code ↔ Opencode Agent

**Версия:** 1.0  
**Дата:** 2026-03-02  
**Статус:** ✅ УТВЕРЖДЁН

---

## 🎯 РОЛИ И ОТВЕТСТВЕННОСТЬ

### Qwen Code (локально, MacBook менеджера)

**Роль:** Senior инженер-архитектор, технический лидер

**Обязанности:**
- ✅ Написание и исправление кода
- ✅ Коммит и пуш кода на GitHub
- ✅ Создание инструкций для Opencode Agent
- ✅ Код-ревью отчётов от Opencode Agent
- ✅ Исправление багов на основе отчётов
- ✅ Принятие архитектурных решений

**НЕ ДЕЛАЕТ:**
- ❌ Не подключается к серверу напрямую
- ❌ Не загружает код на сервер
- ❌ Не выполняет команды на сервере
- ❌ Не тестирует на продакшене

---

### Opencode Agent (VDS, сервер)

**Роль:** SRE/DevOps инженер, тестировщик

**Обязанности:**
- ✅ Получение инструкций из `exchange/`
- ✅ Загрузка кода с GitHub на сервер (`git pull`)
- ✅ Деплой и перезапуск сервисов
- ✅ Тестирование функционала на сервере
- ✅ Мониторинг логов и состояния
- ✅ Написание отчётов в `exchange/toQwen.md`
- ✅ Диагностика проблем по заданию

**НЕ ДЕЛАЕТ:**
- ❌ Не пишет код самостоятельно
- ❌ Не изменяет код без инструкции
- ❌ Не пушит в GitHub
- ❌ Не принимает архитектурных решений

---

## 🔄 РАБОЧИЙ ПРОЦЕСС (Workflow)

### Этап 1: Разработка (Qwen Code)

```
1. Менеджер ставит задачу → Qwen Code
2. Qwen Code пишет/исправляет код
3. Qwen Code делает коммит
4. Qwen Code пушит на GitHub
```

**Результат:** Код на GitHub в ветке `main`

---

### Этап 2: Инструкция (Qwen Code)

```
1. Qwen Code создаёт инструкцию
2. Сохраняет в /tmp/toOpencode_*.md
3. Отправляет на сервер через SCP
   → /var/www/tarot-miniapp/exchange/toOpencode_*.md
```

**Содержание инструкции:**
- Приоритет (КРИТИЧЕСКИЙ/ВЫСОКИЙ/СРЕДНИЙ)
- Задача (что сделать)
- Команды для выполнения (git pull, pm2 restart, etc.)
- Проверки (что проверить после деплоя)
- Формат отчёта (куда и как писать результат)

---

### Этап 3: Деплой (Opencode Agent)

```
1. Opencode Agent читает инструкцию из exchange/
2. Выполняет git pull origin main
3. Перезапускает сервисы (pm2 restart)
4. Проверяет статус (pm2 status, curl API)
```

**Результат:** Код на сервере обновлён

---

### Этап 4: Тестирование (Opencode Agent)

```
1. Opencode Agent выполняет тесты из инструкции
2. Проверяет логи (pm2 logs)
3. Проверяет API (curl)
4. Проверяет функционал (ручное тестирование)
5. Записывает результаты в отчёт
```

---

### Этап 5: Отчёт (Opencode Agent)

```
1. Opencode Agent пишет отчёт
2. Сохраняет в exchange/toQwen.md
3. Qwen Code читает отчёт
```

**Формат отчёта:**
```markdown
# Отчёт Opencode Agent

## Задача: [название]

## Выполнено:
- [ ] пункт 1
- [ ] пункт 2

## Тесты:
- Тест 1: ✅/❌
- Тест 2: ✅/❌

## Проблемы:
1. [описание]

## Логи:
[ключевые сообщения]

## Статус: [ГОТОВО / ТРЕБУЕТ ИСПРАВЛЕНИЯ]
```

---

### Этап 6: Ревью и исправление (Qwen Code)

```
1. Qwen Code читает отчёт из exchange/toQwen.md
2. Анализирует проблемы
3. Пишет исправления
4. Коммитит и пушит
5. Создаёт новую инструкцию на тестирование
```

**Цикл повторяется до статуса "ГОТОВО"**

---

## 📁 СТРУКТУРА ПАПОК

### `/var/www/tarot-miniapp/exchange/`

| Файл | Назначение | Кто пишет | Кто читает |
|------|------------|-----------|------------|
| `toOpencode_*.md` | Инструкции для Opencode | Qwen Code | Opencode Agent |
| `toQwen.md` | Отчёты для Qwen Code | Opencode Agent | Qwen Code |

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА

### 1. Разделение ответственности

```
Qwen Code → GitHub (код)
Opencode Agent → Сервер (деплой + тесты)
```

**Запрещено:**
- ❌ Qwen Code подключается к серверу для деплоя
- ❌ Opencode Agent пишет код без инструкции
- ❌ Прямые изменения на сервере без git pull

---

### 2. Инструкции через exchange/

**Все инструкции только через:**
```
/var/www/tarot-miniapp/exchange/toOpencode_*.md
```

**Запрещено:**
- ❌ Устные инструкции
- ❌ Инструкции вне exchange/
- ❌ Изменение кода без письменной инструкции

---

### 3. Отчёты в toQwen.md

**Все отчёты только в:**
```
/var/www/tarot-miniapp/exchange/toQwen.md
```

**Формат:**
- Чёткий статус (ГОТОВО / ТРЕБУЕТ ИСПРАВЛЕНИЯ)
- Список выполненных задач
- Найденные проблемы
- Логи (ключевые сообщения)

---

### 4. Безопасность

**Qwen Code:**
- ✅ Не хранит ключи API в коде
- ✅ Не пишет токены в инструкциях
- ✅ Использует .env на сервере

**Opencode Agent:**
- ✅ Не передаёт .env файлы
- ✅ Не показывает токены в отчётах
- ✅ Маскирует чувствительные данные

---

## 📊 ПРИМЕР ЦИКЛА РАЗРАБОТКИ

### Задача: Исправить кнопки в админке

**1. Qwen Code пишет исправление:**
```bash
git add -A
git commit -m "fix: кнопки не работали"
git push origin main
```

**2. Qwen Code создаёт инструкцию:**
```bash
cat > /tmp/toOpencode_buttonfix.md << 'EOF'
# Исправление кнопок

## Деплой:
git pull origin main
pm2 restart tarot-server

## Тесты:
1. Проверить кнопки "Тарологи"
2. Проверить кнопки "Транзакции"
3. Проверить модальные окна

## Отчёт:
exchange/toQwen.md
EOF

scp /tmp/toOpencode_buttonfix.md root@server:/var/www/tarot-miniapp/exchange/
```

**3. Opencode Agent выполняет:**
```bash
cd /var/www/tarot-miniapp
git pull origin main
pm2 restart tarot-server

# Тесты
# ...

# Отчёт
cat > exchange/toQwen.md << 'EOF'
# Отчёт

## Тесты:
- Кнопки "Тарологи": ✅
- Кнопки "Транзакции": ✅
- Модальные окна: ✅

## Статус: ГОТОВО
EOF
```

**4. Qwen Code читает отчёт:**
```bash
ssh root@server "cat /var/www/tarot-miniapp/exchange/toQwen.md"
```

**5. Если статус "ГОТОВО" → задача закрыта**  
**Если статус "ТРЕБУЕТ ИСПРАВЛЕНИЯ" → повторить цикл**

---

## 🔧 ИНСТРУМЕНТЫ

### Qwen Code использует:

| Инструмент | Назначение |
|------------|------------|
| `git commit/push` | Отправка кода на GitHub |
| `scp` | Отправка инструкций на сервер |
| `ssh cat` | Чтение отчётов с сервера |

---

### Opencode Agent использует:

| Инструмент | Назначение |
|------------|------------|
| `git pull` | Загрузка кода с GitHub |
| `pm2 restart/status/logs` | Управление сервисом |
| `curl` | Проверка API |
| `sqlite3` | Проверка БД |
| `cat > exchange/` | Написание отчётов |

---

## 📈 МЕТРИКИ КАЧЕСТВА

### Для Qwen Code:

| Метрика | Цель |
|---------|------|
| Код без багов | > 95% |
| Инструкции понятные | 100% |
| Время на исправление | < 30 мин |

---

### Для Opencode Agent:

| Метрика | Цель |
|---------|------|
| Деплой без ошибок | 100% |
| Отчёты полные | 100% |
| Тесты пройдены | > 90% |

---

## 🚀 АВТОМАТИЗАЦИЯ

### Планируется:

1. **GitHub Actions** — автоматический деплой при пуше
2. **Health checks** — автоматическая проверка API после деплоя
3. **Alerts** — уведомления о критических ошибках

---

## 📞 ЭСКАЛАЦИЯ

### Если проблема не решается после 3 циклов:

1. Qwen Code создаёт детальную диагностику
2. Opencode Agent выполняет пошаговую проверку
3. Если не помогает → эскалация менеджеру

---

## ✅ ЧЕК-ЛИСТ ПРИНЯТИЯ ПРОТОКОЛА

**Qwen Code подтверждает:**
- [ ] Понимаю свою роль (архитектор + разработчик)
- [ ] Не подключаюсь к серверу для деплоя
- [ ] Пишу инструкции через exchange/
- [ ] Читаю отчёты из toQwen.md
- [ ] Исправляю код на основе отчётов

**Opencode Agent подтверждает:**
- [ ] Понимаю свою роль (SRE + тестировщик)
- [ ] Загружаю код через git pull
- [ ] Выполняю инструкции из exchange/
- [ ] Пишу отчёты в toQwen.md
- [ ] Не пишу код без инструкции

---

**ПРОТОКОЛ УТВЕРЖДЁН:** 2026-03-02  
**ВЕРСИЯ:** 1.0  
**СТАТУС:** ✅ ДЕЙСТВУЮЩИЙ
