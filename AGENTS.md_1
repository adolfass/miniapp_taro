# AGENTS.md — Полный контекст для разработки Tarot Mini App

Этот файл содержит всю необходимую информацию для AI-ассистента, чтобы разработать Telegram Mini App для гадания на картах Таро. Читайте внимательно, все требования важны.

---

## 📋 1. ОБЩАЯ ИНФОРМАЦИЯ

**Проект:** Telegram Mini App для гадания на картах Таро  
**Основная идея:** Мистическое приложение с фокусом на визуальное восприятие карт, красивые анимации и карточную механику. Пользователь взаимодействует с колодой, выбирает расклад и переворачивает карты, получая толкования.

**Целевая платформа:** Telegram (Mobile + Desktop) через WebApp API.  
**Ключевые особенности:**
- Оригинальная рубашка в мистическом стиле, вдохновленном эстетикой Таро Алистера Кроули (но не копирующая её).
- Звук перетасовки карт, как в казино.
- Два варианта раскладов (ежедневный и на ситуацию) с возможностью поделиться результатом с тарологом.
- Альтернативный способ активации расклада на ситуацию – встряхивание телефона (имитация броска костей).
- Плавные анимации, ориентированные на 120 Гц экраны современных устройств.

---

## 🎨 2. ДИЗАЙН И ВИЗУАЛЬНАЯ КОНЦЕПЦИЯ

- **Стиль:** Мистический, минималистичный, с акцентом на карты.
- **Цветовая палитра:**
  - Фон: `#1a0b2e` (глубокий фиолетовый)
  - Акценты: `#d4af37` (золото)
  - Текст: светлые тона с полупрозрачностью для атмосферы.
- **Шрифты:** 
  - Заголовки: `'Cormorant Garamond', serif` или `'Cinzel', serif`
  - Основной текст: системный шрифт с засечками для читаемости.
- **Атмосферные эффекты:**
  - Фон с легким градиентом и мерцающими звездами (canvas или CSS).
  - Свечение вокруг карт при наведении/выборе.
  - Частицы (sparkles) при перевороте карты.
- **Рубашка карт:** Единый дизайн для всех карт. Мистический узор: переплетающиеся линии, розы, кресты, змеи, космические элементы. Цвета: тёмно-синий/фиолетовый фон, золотые линии. Должна выглядеть оригинально, без копирования конкретных колод.

---

## 🎴 3. МЕХАНИКА РАБОТЫ С КАРТАМИ

### 3.1. Колода
- Полная колода Таро: 78 карт (22 Старших Аркана + 56 Младших).
- У каждой карты есть:
  - Уникальная лицевая сторона (изображение).
  - Оригинальная рубашка (общий дизайн).

### 3.2. Взаимодействие
1. **Главный экран:** Колода лежит рубашками вверх (веером или стопкой). Присутствует иконка настроек (для включения/отключения звука).
2. **Тасовка:** При нажатии на колоду запускается анимация перетасовки (карты меняются местами) и **проигрывается звук перетасовки карт, как в казино**. Звук можно отключить в настройках.
3. **Выбор расклада:** После тасовки появляется меню с двумя вариантами:
   - **Ежедневный расклад**
   - **Расклад на ситуацию**
4. **Ежедневный расклад:**
   - 3 случайные карты.
   - Карты выкладываются рубашкой вверх, затем пользователь переворачивает их (тап по карте).
   - После переворота под каждой картой появляется краткое значение (2–3 предложения), сгенерированное на основе алгоритмов Таро.
   - Внизу экрана появляется кнопка **«Поделиться с тарологом»** — при нажатии копирует в буфер обмена текст с результатами расклада и предлагает отправить другу/тарологу (либо использует `tg.sendData` для отправки боту).
5. **Расклад на ситуацию:**
   - Пользователю предлагается в течение 5 секунд сосредоточиться на волнующей ситуации (таймер с обратным отсчётом).
   - После таймера появляется интерфейс с двумя способами активации расклада:
     - **Кнопка «Сделать расклад»**.
     - **Встряхивание телефона** (имитация броска костей). На мобильных устройствах пользователь может просто потрясти телефон, чтобы запустить выбор карт.
   - При активации случайным образом выбираются 7 карт.
   - Карты выкладываются в два ряда: **три карты вверху**, **четыре карты внизу** (визуально как 2 ряда).
   - Каждая карта переворачивается пользователем.
   - Под картами появляется простая трактовка (общее значение расклада) с учётом позиций.
   - Внизу экрана кнопка **«Поделиться с тарологом»** — аналогично ежедневному раскладу.
6. **Завершение:** Возможность сделать новый расклад (кнопка «Новый расклад»).

### 3.3. Анимации (CSS/JS)
- **Переворот:** `transform: rotateY(180deg)` с `transition` и аппаратным ускорением (`translateZ(0)`).
- **Тасовка:** Быстрая смена z-index и позиций карт (имитация перемешивания) + звук.
- **Появление карт:** Плавное выдвижение с эффектом «магии».
- **Частицы:** Использовать canvas для создания искр в момент переворота.
- **Таймер 5 секунд:** Круговая анимация или цифровой отсчёт.

---

## 📱 4. ИНТЕРФЕЙС И ЭКРАНЫ

1. **Главный экран** — колода, иконка настроек.
2. **Экран выбора расклада** — две большие стилизованные кнопки.
3. **Экран ежедневного расклада** — 3 карты в ряд.
4. **Экран расклада на ситуацию** — 3 карты сверху, 4 снизу.
5. **Экран результата** — после переворота всех карт показывается текст трактовки и кнопка «Поделиться с тарологом».

### Элементы управления
- Кнопка «Перетасовать» (стилизована под старинную печать).
- Кнопка «Новый расклад».
- Кнопка «Поделиться с тарологом».
- Переключатель звука (в настройках).

---

## 🧩 5. ТЕХНИЧЕСКИЙ СТЕК И ОПТИМИЗАЦИЯ

### 5.1. Выбор технологий (для максимальной производительности на 120 Гц)
- **Vanilla JavaScript (ES6+)** — никаких фреймворков, чтобы обеспечить полный контроль над анимациями и избежать лишних ререндеров.
- **CSS3 с аппаратным ускорением:** `transform: translateZ(0)`, `will-change: transform` для плавности на 120 Гц.
- **Canvas** для частиц (отдельный слой, не блокирует основной поток).
- **WebP** для всех изображений (экономия трафика, ускорение загрузки).
- **Vite** как сборщик — быстрая разработка и оптимальные продакшн-настройки.

### 5.2. Структура проекта

tarot-mini-app/
├── public/
│ ├── index.html
│ ├── css/
│ │ └── style.css
│ ├── js/
│ │ ├── main.js # точка входа, инициализация Telegram WebApp
│ │ ├── cards.js # массив с данными всех 78 карт
│ │ ├── deck.js # логика колоды (перетасовка, выдача)
│ │ ├── spreads.js # логика раскладов (позиции)
│ │ ├── animations.js # функции для анимаций и canvas-эффектов
│ │ ├── sound.js # управление звуком (переключатель, проигрывание shuffle.mp3)
│ │ └── shake.js # детекция встряхивания телефона
│ └── assets/
│ ├── card-backs/
│ │ └── back.webp # единая рубашка для всех карт
│ ├── card-faces/ # изображения карт (группировать по major/ minor/ в формате webp)
│ └── sounds/
│ └── shuffle.mp3 # звук перетасовки карт
├── server.js # (опционально) простой сервер для раздачи статики
├── package.json
├── vite.config.js # конфигурация Vite
└── README.md


### 5.3. Описание ключевых JS-модулей
- **main.js** — инициализация Telegram WebApp, определение темы, рендеринг главного экрана, обработка навигации.
- **cards.js** — экспорт массива объектов карт (см. п. 6).
- **deck.js** — функции для тасовки, получения случайных карт, управления колодой.
- **spreads.js** — функции для выкладки карт в соответствии с выбранным раскладом, генерация текста трактовки.
- **animations.js** — функции для анимации переворота, частиц, свечения.
- **sound.js** — загрузка и воспроизведение shuffle.mp3, переключатель звука.
- **shake.js** — детекция встряхивания телефона (с обработкой разрешений iOS).

### 5.4. Оптимизация для 120 Гц
- Использовать только `transform` и `opacity` для анимаций — они не вызывают reflow.
- Добавлять `transform: translateZ(0)` и `will-change: transform` на карточки.
- Для Canvas: использовать `requestAnimationFrame`, очищать только изменяемые области, вынести canvas в отдельный слой с `transform: translateZ(0)`.
- Динамический импорт модулей раскладов (чтобы не грузить лишний код до выбора).
- Preload критических ресурсов: рубашка, первые карты, CSS.
- Service Worker для кэширования статики.

---

## 🃏 6. ДАННЫЕ КАРТ (cards.js)

Каждая карта представляется объектом. Для MVP можно начать со Старших Арканов (22 карты), затем добавить Младшие.

```javascript
export const cards = [
  {
    id: 0,
    name: "The Fool",
    name_ru: "Шут",
    arcana: "major",
    suit: null,
    image: "fool.webp",         // путь относительно assets/card-faces/major/
    description: "Начало пути, спонтанность, вера в лучшее. Призыв довериться миру и сделать шаг в неизвестность.",
    keywords: ["новое начало", "приключение", "доверие"]
  },
  // ... остальные карты
];

Для Младших Арканов добавить поля suit (wands, cups, swords, pentacles) и rank (ace, 2–10, page, knight, queen, king).

Трактовки для карт должны быть краткими (2–3 предложения). Для общих описаний раскладов (3 и 7 карт) можно комбинировать ключевые слова карт в шаблонные фразы.

🖼️ 7. ИЗОБРАЖЕНИЯ КАРТ
Формат: WebP для оптимального сжатия.

Старшие Арканы: уникальные символические рисунки в мистическом стиле, не копирующие существующие колоды.

Младшие Арканы: стилизованные символы мастей на фоне с мистическим узором, соответствующим рубашке.

Рубашка: единый файл back.webp с детальным узором (золото по тёмно-синему/фиолетовому).

🔌 8. ИНТЕГРАЦИЯ С TELEGRAM
Подключить скрипт https://telegram.org/js/telegram-web-app.js.

Использовать window.Telegram.WebApp для:

Растягивания на весь экран: tg.expand()

Определения цветовой схемы: tg.colorScheme (светлая/тёмная)

Отправки данных боту: tg.sendData(data) — для отправки результатов расклада (бот должен быть настроен).

Тактильного отклика: tg.HapticFeedback.impactOccurred('medium') при распознавании встряхивания.

📱 9. РЕАЛИЗАЦИЯ ВСТРЯХИВАНИЯ (shake.js)
javascript
// shake.js
export function initShake(callback, threshold = 15, timeout = 1000) {
  let lastTime = 0;
  let lastX, lastY, lastZ;

  function handleMotion(event) {
    const acceleration = event.accelerationIncludingGravity;
    if (!acceleration) return;

    const { x, y, z } = acceleration;
    const currentTime = Date.now();

    if (lastX !== undefined) {
      const deltaX = Math.abs(x - lastX);
      const deltaY = Math.abs(y - lastY);
      const deltaZ = Math.abs(z - lastZ);
      const totalDelta = deltaX + deltaY + deltaZ;

      if (totalDelta > threshold && (currentTime - lastTime) > timeout) {
        callback();
        lastTime = currentTime;
      }
    }

    lastX = x;
    lastY = y;
    lastZ = z;
  }

  if (window.DeviceMotionEvent) {
    if (typeof DeviceMotionEvent.requestPermission === 'function') {
      DeviceMotionEvent.requestPermission()
        .then(permissionState => {
          if (permissionState === 'granted') {
            window.addEventListener('devicemotion', handleMotion);
          }
        })
        .catch(console.error);
    } else {
      window.addEventListener('devicemotion', handleMotion);
    }
  }
}
Интеграция: после 5-секундного таймера вызываем initShake, передавая функцию запуска расклада. На десктопах функция недоступна — показываем только кнопку.

⚡ 10. ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ К КОДУ
Код должен быть чистым, хорошо прокомментированным, без лишних зависимостей.

Использовать ES6-модули (import/export).

Обрабатывать ошибки (например, если звук не загрузился).

Предусмотреть мок Telegram API для разработки в браузере (см. п. 11).

Для iOS обязательно обрабатывать DeviceMotionEvent.requestPermission().

Встряхивание — дополнительный способ; кнопка всегда доступна.

При копировании текста для шаринга использовать tg.showAlert для подтверждения.

💻 11. ЛОКАЛЬНАЯ РАЗРАБОТКА НА MAC
Для удобства разработки можно использовать мок Telegram API:

javascript
// main.js - временно для разработки
if (import.meta.env.DEV) {
  window.Telegram = {
    WebApp: {
      initData: 'user=test',
      initDataUnsafe: { user: { id: 123, first_name: 'Test' } },
      colorScheme: 'dark',
      themeParams: {},
      expand: () => {},
      ready: () => {},
      showAlert: (msg) => alert(msg),
      HapticFeedback: { impactOccurred: () => {} }
    }
  };
}
Для тестирования в реальном Telegram используйте туннелирование (ngrok) или self-signed SSL. Подробности в DEPLOY.md.












