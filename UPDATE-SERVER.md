# 🔄 Обновление конфигурации на сервере

## Важно! Изменилась структура развёртывания

Теперь Nginx раздаёт файлы из папки `dist/`, а не из корня проекта.

---

## 📋 Шаги обновления

### **Шаг 1: Подключитесь к серверу**

```bash
ssh root@89.125.59.117
```

---

### **Шаг 2: Обновите код из репозитория**

```bash
cd /var/www/tarot-miniapp
git pull origin main
```

---

### **Шаг 3: Пересоберите проект**

```bash
npm install
npm run build
```

---

### **Шаг 4: Обновите конфиг Nginx**

```bash
# Откройте конфиг
nano /etc/nginx/sites-available/tarot-miniapp
```

**Измените строку `root`:**

```nginx
# Было:
root /var/www/tarot-miniapp;

# Стало:
root /var/www/tarot-miniapp/dist;
```

**Сохраните:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

### **Шаг 5: Проверьте и перезагрузите Nginx**

```bash
# Проверка конфига
nginx -t

# Перезагрузка
systemctl reload nginx
```

---

### **Шаг 6: Проверьте сайт**

```bash
# Проверка
curl -I https://goldtarot.ru

# Должно быть: HTTP/1.1 200 OK
```

**Откройте в браузере:**
```
https://goldtarot.ru
```

---

## ✅ Всё готово!

Теперь при каждом деплое через `/var/www/deploy.sh` файлы будут автоматически собираться в `dist/` и Nginx будет их раздавать.

---

## 📊 Структура файлов

```
/var/www/tarot-miniapp/
├── dist/              ← Отсюда Nginx раздаёт файлы
│   ├── index.html
│   └── assets/
│       ├── index-xxxxx.css
│       └── index-xxxxx.js
├── public/            ← Исходный код
├── src/
└── package.json
```

---

## 🚀 Быстрое обновление (в будущем)

```bash
/var/www/deploy.sh
```

Этот скрипт автоматически:
1. Обновит код из GitHub
2. Установит зависимости
3. Соберёт проект в `dist/`
4. Перезагрузит Nginx
